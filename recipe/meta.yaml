{% set name = "napari-czann-segment" %}
{% set version = "0.0.19" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name | replace('-', '_') }}-{{ version }}.tar.gz
  sha256: 0a3eeb34dff1a151c88255ef456728f9fbf59a6cc988ece6e12152ab9913bae2

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.8
    - setuptools-scm
  run:
    - aicsimageio
    - czmodel >=5
    - cztile
    - dask-core
    - magicgui
    - napari
    - numpy
    - onnxruntime
    - python >=3.8
    - pytorch
    - qtpy
    - scikit-image
    - tifffile >=2023.4.12
    - tqdm

test:
  imports:
    - napari_czann_segment
  commands:
    - pip list
    # - pip check 
  requires:
    - pip
    - pyqt

about:
  home: https://pypi.org/project/napari-czann-segment
  summary: Segmention using DeepLearning ONNX models packaged as *.czann files
  license: BSD-3-Clause
  license_file: LICENSE
  dev_url: https://github.com/sebi06/napari-czann-segment

extra:
  recipe-maintainers:
    - goanpeca
    - jaimergp
    - sebi06
